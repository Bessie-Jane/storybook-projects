import { profilePageDisplay } from "./profilePage"


export default {
    title: 'Test_Six/ProfilePage'
}

const Template = ({}) => profilePageDisplay()

export const profilePage_Display = Template.bind({})